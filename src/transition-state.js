export const PRE_DELAY  = 220;  // ms to wait before image starts moving
export const MOVE_MS   = 800;  // ms for the image to travel to destination

export const transitionState = {
  slug: null,
};
